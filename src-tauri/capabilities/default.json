{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline shell permissions. Every capability is added deliberately: the engine, not the webview, owns filesystem and network access.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
